# 프로젝트 기술 문서

> 프로젝트의 기술 스택, 아키텍처, 주요 구조를 설명합니다.

---

## 1. 기술 스택

### Backend
| 항목 | 버전 |
|------|------|
| Framework | {프레임워크명} {버전} |
| Language | {언어} {버전} |
| ORM/Mapper | {ORM/매퍼명} {버전} |
| Database | {DB명} |
| Build Tool | {빌드 도구} |
| 인증 | {인증 방식} |

### Frontend
| 항목 | 버전 |
|------|------|
| Framework | {프레임워크명} {버전} |
| Language | {언어} {버전} |
| Bundler | {번들러명} {버전} |
| State Management | {상태관리 라이브러리} {버전} |
| CSS | {CSS 방식} |

---

## 2. 서버 아키텍처

```
{프로젝트별 서버 구성도}

예시:
┌─────────────┐    ┌─────────────┐
│   Backend   │    │  Database   │
│  ({기술})   │───▶│  ({DB명})   │
└─────────────┘    └─────────────┘
       │
       ▼
┌─────────────┐
│  Frontend   │
│  ({기술})   │
└─────────────┘
```

---

## 3. 주요 디렉토리 구조

```
{프로젝트 루트}/
├── {백엔드 소스 경로}/
│   ├── {컨트롤러/핸들러}/
│   ├── {서비스/비즈니스 로직}/
│   ├── {데이터 접근 계층}/
│   ├── {DTO/모델}/
│   └── {설정}/
├── {프론트엔드 소스 경로}/
│   ├── {컴포넌트}/
│   ├── {상태관리}/
│   ├── {라우터}/
│   ├── {타입 정의}/
│   ├── {API 서비스}/
│   └── {유틸리티}/
└── docs/
```

---

## 4. 데이터베이스 구성

| DB | 용도 | 접속 방식 |
|----|------|-----------|
| {DB1} | {주요 데이터} | {직접 연결} |

---

## 5. 주요 API 엔드포인트

| 경로 | 설명 | 핸들러 |
|------|------|--------|
| `/api/{resource}` | {설명} | `{Handler}` |

---

## 6. 배포 환경

| 환경 | 프로파일 | 설명 |
|------|----------|------|
| 개발 | `dev` | 로컬 개발 |
| 스테이징 | `staging` | 테스트 |
| 운영 | `prod` | 실서비스 |

---

> 이 문서는 프로젝트에 맞게 내용을 채워 사용합니다.
